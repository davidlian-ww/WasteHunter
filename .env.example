# Copy this to .env for local overrides (optional — defaults work without it)

# On Render this is set automatically via render.yaml.
# Locally, leave blank — DB lands in the timwood-dashboard directory.
DATA_DIR=

# SharePoint form URL for the /share page
# SHAREPOINT_FORM_URL=https://forms.office.com/your-form-id
